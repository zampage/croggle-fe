{% extends 'layout/default.html.twig' %}

{% block pageTitle %}{{ page.title }}{% endblock %}

{% block content %}
    {{ page.content }}
    {% for module in page.collection() %}
        {{module.content}}
    {% endfor %}
{% endblock %}
